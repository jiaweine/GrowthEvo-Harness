# Benchmark Evidence Note

Public-dataset benchmark numbers are added to the project overview only when the result can be traced to a reproducible experiment artifact containing the data fingerprint, split protocol, seed, preprocessing, model and estimator configuration, metric definition, and output table.

Dataset adapters, trainer interfaces, or successful unit tests are implementation evidence. They are not by themselves evidence of real-world uplift or superior offline-RL performance.
