[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "growthevo-harness"
version = "0.1.0"
description = "Causal reinforcement learning runtime for autonomous user growth agents"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "GrowthEvo contributors" }]
dependencies = []

[project.scripts]
growthevo-locked-ope = "growthevo.bench.locked_ope_cli:main"
growthevo-locked-targeting = "growthevo.bench.locked_targeting_cli:main"

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "pytest-cov>=5.0",
]
obd = [
  "sb-obp==0.5.10; python_version < '3.13'",
]
criteo = [
  "lightgbm==4.7.0",
  "numpy==1.26.4",
  "pandas==2.3.3",
  "scikit-learn==1.9.0",
]

[tool.setuptools.packages.find]
include = ["growthevo*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.coverage.run]
branch = true
source = ["growthevo"]
